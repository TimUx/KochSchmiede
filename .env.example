# PostgreSQL password (change in production!)
POSTGRES_PASSWORD=secret

# JWT secret key (use a long random string in production)
SECRET_KEY=change-me-please-use-a-long-random-string

# Optional: your domain for CORS
DOMAIN=localhost
